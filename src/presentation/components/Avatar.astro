---
import { Image } from 'astro:assets';
import avatar from '../../assets/avatar.jpg';

interface Props {
	size?: number;
}

const { size = 120 } = Astro.props;
---

<div class="avatar-frame" style={`--avatar-size: ${size}px`}>
	<Image src={avatar} alt="Foto de Nathan Amorim" width={size * 3} height={size * 3} quality={90} />
</div>

<style>
	.avatar-frame {
		width: var(--avatar-size);
		height: var(--avatar-size);
		flex-shrink: 0;
		border-radius: 50%;
		padding: 3px;
		background: linear-gradient(135deg, var(--cyan), var(--green));
		box-shadow: 0 0 24px rgba(37, 99, 235, 0.25);
	}

	.avatar-frame :global(img) {
		width: 100%;
		height: 100%;
		border-radius: 50%;
		object-fit: cover;
		display: block;
		background: var(--panel);
	}

	@media print {
		.avatar-frame {
			box-shadow: none;
			background: #ccc;
		}
	}
</style>
